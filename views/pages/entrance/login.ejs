<div id="login" v-cloak>
  <div class="container">
    <h1 class="text-center">Bienvenido al sistema :)</h1>
    <div class="login-form-container">
      <hr/>
      <ajax-form action="login" :syncing.sync="syncing" :cloud-error.sync="cloudError" :form-data="formData" :form-rules="formRules" :form-errors.sync="formErrors" @submitted="submittedForm()">
        <div class="form-group">
          <input type="email" class="form-control" placeholder="Correo" :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" autocomplete="email" focus-first>
          <div class="invalid-feedback" v-if="formErrors.emailAddress">Por favor ingreses in correo valido.</div>
        </div>
        <div class="form-group">
          <input type="password" class="form-control" placeholder="Contraseña" :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password" autocomplete="current-password">
          <div class="invalid-feedback" v-if="formErrors.password">Por favor ingrese una contraseña.</div>
        </div>
        <div class="form-group form-check">
          <input class="form-check-input" type="checkbox" id="remember" name="rememberMe" v-model="formData.rememberMe"/>
          <label class="form-check-label" for="remember">Recuerdame</label>
        </div>
        <p class="text-danger" v-if="cloudError==='badCombo'"><small>Los datos ingresdos no corresponden a ningun usuario. Por favor revise el correo y contraseña e intentelo de nuevo.</small></p>
        <p class="text-danger" v-else-if="cloudError"><small>Ocurrio un error al proceesar su pedido :(. Por favor revise la informacion, o  <a href="/contact">contactese con el soporte</a> si el problema persiste.</small></p>
        <div class="form-group">
          <ajax-button :syncing="syncing" class="btn-dark btn-lg btn-block">Ingresar</ajax-button>
        </div>
      </ajax-form>
      <p class="text-center"><a href="/password/forgot">Olvido su contraseña?</a></p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
